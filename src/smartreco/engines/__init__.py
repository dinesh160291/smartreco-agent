"""Deterministic platform engines — pure functions, no AI, no I/O.

Engines determine truth; Decision Policies (config/policies.yaml via the single
loader) authorize actions; AI only communicates outcomes (docs/core/10).
"""
